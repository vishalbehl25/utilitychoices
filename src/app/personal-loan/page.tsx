import type { Metadata } from 'next';
import { PAGE_METADATA } from '@/constants/metadata';

export const metadata: Metadata = PAGE_METADATA.personalLoan;

export { default } from './PersonalLoansClient';
